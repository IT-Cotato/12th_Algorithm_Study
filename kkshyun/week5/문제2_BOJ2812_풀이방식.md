## BOJ 2812 문제 요약

- 길이 N의 숫자에서 K개 자릿수를 제거해 가장 큰 수를 만들어 출력
- 숫자의 원래 순서는 유지, 제거만 가능

---

## 아이디어

- 스택 + 그리디
    - 왼쪽부터 보면서, 스택의 맨 뒤(바로 왼쪽 자리)가 현재 숫자보다 작고 K>0이면 그 자리를 지워 더 큰 숫자가 앞에 오도록 만듦
- 끝까지 봤는데도 K가 남아 있으면, 더 키울 방법이 없으므로 가장 덜 중요한 오른쪽 끝에서 K개 제거

---

## 알고리즘 단계

- 숫자를 문자열로 읽고 한 자리씩 순회
    - while (스택 not empty && K>0 && 스택뒤 < 현재숫자) → 스택 뒤를 pop, K−−
    - 현재 숫자를 스택 뒤에 push
- 순회가 끝난 뒤 K>0이면 스택 뒤에서 K번 pop
- 스택에 남은 숫자들을 이어서 출력

---

## 코드

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.util.ArrayDeque;
import java.util.Deque;

public class Main {
    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        String[] nm = br.readLine().split(" ");
        int N = Integer.parseInt(nm[0]);
        int K = Integer.parseInt(nm[1]);
        char[] digits = br.readLine().trim().toCharArray();

        Deque<Character> stack = new ArrayDeque<>(N);

        for (char d : digits) {
            while (!stack.isEmpty() && K > 0 && stack.peekLast() < d) {
                stack.pollLast(); // 더 작은 앞자리를 제거해 큰 수를 앞으로
                K--;
            }
            stack.addLast(d);
        }

        // 아직 제거 횟수가 남았으면 뒤에서 제거
        while (K > 0) {
            stack.pollLast();
            K--;
        }

        for (char c : stack) {
            bw.write(c+"");
        }
        bw.flush();
        bw.close();
    }
}
```

---

## 시간 복잡도

- 메모리: 30460 KB, 시간: 332 ms